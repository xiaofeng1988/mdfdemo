Ext.define(
	"",//定义的类名
	{
		extend:"",//继承某个类
		requires:['a','b'],//列表中的类实例在这个类之前加载
		uses:['a','b'],//列表中的类和该类一起被加载，但不一定是在该类创建之前加载，保证在Ext.onReady监听方法被调用的时候这些类可用
		...
	}//被定义的类（具有了类的一切特性，可以使用类的方法，属性等）
);